import React, { useState, useEffect } from "react";

import { PlanScreen } from "./PlanScreen";

export const PlanScreenContainer = ({navigation}:any) => {
  return <PlanScreen navigation={navigation} />;
};
